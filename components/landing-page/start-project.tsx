"use client";

import ShopifyProjectForm from "./shopify-project-form";

/** Full-page Shopify Stage 1 intake (no site chrome). */
export default function StartProject() {
  return <ShopifyProjectForm />;
}
